# Media manifest

Place supplied lesson videos, audio, and recording assets here. The UI references media by stable logical names; missing files render a friendly fallback instead of breaking the lesson.
