import {useState} from 'react'; import {words} from '../../data/lesson'; import {MediaFallback} from '../../components/ui/MediaFallback';
export function VocabularyPage(){const [idx,setIdx]=useState(0);const w=words[idx];return <div className="vocab-page"><div className="vocab-head"><div><span className="eyebrow">词句宝库</span><h1>课课清</h1></div><div className="subnav"><button className="active">课课清</button><button>拼写大师</button><button>滚动复习</button><button>词汇大闯关</button></div></div><div className="word-layout"><aside className="word-list"><span>本课词汇 · 5</span>{words.map((x,i)=><button className={i===idx?'active':''} onClick={()=>setIdx(i)} key={x.word}><b>{x.word}</b><small>{x.meaning}</small></button>)}<button className="spell-link">✦ 去玩拼写大师 →</button></aside><section className="word-card"><div className="word-image"><img src={w.image} onError={e=>(e.currentTarget.style.display='none')} /><MediaFallback label="单词卡插画"/></div><div className="word-info"><span className="word-tag">FRUIT · L1</span><h2>{w.word}</h2><p className="phonetic">{w.phonetic} <button>🔊</button></p><div className="meaning">{w.meaning}</div><div className="spelling">{w.spelling.map((x,i)=><span key={i}>{x}</span>)}</div><p className="sentence">“{w.sentence}”</p><button className="record">🎙 录下你的发音</button></div><div className="card-nav"><button onClick={()=>setIdx((idx+words.length-1)%words.length)}>←</button><span>{idx+1} / {words.length}</span><button onClick={()=>setIdx((idx+1)%words.length)}>→</button></div></section></div></div>}
